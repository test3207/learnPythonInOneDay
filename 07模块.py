# 安装模块：
# pip install {lib}

# 常用语法：
# import {lib}
# import {lib} as {newLibName}
# from {lib} import {func}
# from {lib} import {func} as {newFuncName}

# 引入模块的路径可以通过以下代码查看，主要是pip管理的全局路径和当前目录路径
import sys
print(sys.path)